export const formatCurrency = (amount) => {
  return new Intl.NumberFormat('en-LK', {
    style: 'currency',
    currency: 'LKR',
    minimumFractionDigits: 0, // Apple usually doesn't show decimals for high prices
  }).format(amount);
};
